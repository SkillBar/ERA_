/**
 * Порт чтения из блокчейна (EVM / Solana / и т.д.).
 * Реализация позже: viem/ethers + RPC, индексер, или подпись кошелька на фронте + верификация здесь.
 * Домен и use-case зависят только от этого интерфейса, не от конкретной сети.
 */
export interface ChainReadPort {
  /** Проверка доступности RPC / провайдера (health). */
  isHealthy(): Promise<boolean>
}
